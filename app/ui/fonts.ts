import {Gabarito, Handlee, Gantari} from "next/font/google";

export const gabarito = Gabarito({
  subsets: ['latin'],
  weight: ['400', '700', '800'], })

export const handlee = Handlee({
  weight: ['400'],
  subsets: ['latin'],
})
export const gantari = Gantari({
  weight: ['400'],
  subsets: ['latin'],
})